import React from "react";
import ReactDOM from "react-dom";
import "./input.css"; // Import the Tailwind CSS styles
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
